# How to push your codebase to git

1. create a git repository

2. come back to your project and run the command below in your terminal to innitialize git 

```
git init
```
3. In your terminal run the command below

```
git add .
```